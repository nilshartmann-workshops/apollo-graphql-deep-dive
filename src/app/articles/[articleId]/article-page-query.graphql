query ArticlePage($articleId: ID!) {
  article(articleId: $articleId) {
    id
    requestedAt
    title
    excerpt(maxLength: 120)
    date
    category
    likes
    body
    image {
      uri
      altText
    }
    writer {
      name
    }
  }
}
